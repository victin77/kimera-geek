# URL da API do sistema interno (Railway). Usada SÓ no build de produção.
# Em desenvolvimento (npm run dev) o site cai no fallback localhost:4000.
# Não é segredo: é um endpoint público de leitura, protegido por CORS.
VITE_API_URL=https://kimera-admin-production.up.railway.app
